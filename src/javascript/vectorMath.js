
// Updated at: 2026-08-10T20:18:33.383Z [Pulse 1/24]
function euclideanDistance(a, b) {
  let sum = 0;
  for (let i = 0; i < a.length; i++) {
    sum += Math.pow(a[i] - b[i], 2);
  }
  return Math.sqrt(sum);
}

module.exports = { euclideanDistance };

// Updated at: 2026-08-10T20:18:33.424Z [Pulse 5/24]
function euclideanDistance(a, b) {
  let sum = 0;
  for (let i = 0; i < a.length; i++) {
    sum += Math.pow(a[i] - b[i], 2);
  }
  return Math.sqrt(sum);
}

module.exports = { euclideanDistance };

// Updated at: 2026-08-10T20:18:33.435Z [Pulse 6/24]
function euclideanDistance(a, b) {
  let sum = 0;
  for (let i = 0; i < a.length; i++) {
    sum += Math.pow(a[i] - b[i], 2);
  }
  return Math.sqrt(sum);
}

module.exports = { euclideanDistance };

// Updated at: 2026-08-10T20:18:33.517Z [Pulse 14/24]
function euclideanDistance(a, b) {
  let sum = 0;
  for (let i = 0; i < a.length; i++) {
    sum += Math.pow(a[i] - b[i], 2);
  }
  return Math.sqrt(sum);
}

module.exports = { euclideanDistance };
